<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="start"
    style="@style/screen"
    tools:context=".ui.admin.AdminHomeActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/topAppBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/darknes_red"
            android:theme="@style/ThemeOverlay.Material3.Toolbar.Surface"
            app:menu="@menu/menu_superior" >

            <TextView
                android:id="@+id/toolbar_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sazón Express"
                android:fontFamily="@font/berkshire_swash"
                android:textColor="@color/white"
                android:textSize="30sp"
                android:layout_centerVertical="true" />

        </androidx.appcompat.widget.Toolbar>


        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            style="@style/screen"
            android:layout_height="0dp"
            android:layout_weight="1"/>
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/admin_menu"
        app:headerLayout="@layout/header_nav"
        style="@style/screen"
        android:theme="@style/Theme.Navigation."
        app:itemIconPadding="30dp"
        app:itemTextColor="@color/text_color_menu"
        app:itemIconTint="@color/text_color_menu"/>
</androidx.drawerlayout.widget.DrawerLayout>